[tool.ruff]
target-version = "py312"
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
# Le paquet `app` vit a la racine du depot : pytest doit l'y trouver,
# qu'il soit lance par `pytest` ou par `python -m pytest`.
pythonpath = ["."]
